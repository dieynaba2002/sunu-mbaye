import { ProduitCategPipe } from './produit-categ.pipe';

describe('ProduitCategPipe', () => {
  it('create an instance', () => {
    const pipe = new ProduitCategPipe();
    expect(pipe).toBeTruthy();
  });
});
